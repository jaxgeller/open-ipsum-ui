import { htmlify } from '../../../helpers/htmlify';
import { module, test } from 'qunit';

module('Unit | Helper | htmlify');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = htmlify(42);
  assert.ok(result);
});
