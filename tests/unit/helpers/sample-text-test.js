import { sampleText } from '../../../helpers/sample-text';
import { module, test } from 'qunit';

module('Unit | Helper | sample text');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = sampleText(42);
  assert.ok(result);
});
