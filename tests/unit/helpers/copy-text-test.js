import { copyText } from '../../../helpers/copy-text';
import { module, test } from 'qunit';

module('Unit | Helper | copy text');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = copyText(42);
  assert.ok(result);
});
